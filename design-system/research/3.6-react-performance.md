# Phase 3.6 — React Performance

## Query: bundle suspense memo rerender lazy image
## Source: react-performance.csv | 8 results

### Critical
1. **Dynamic Imports** — Use React.lazy() for heavy components not needed on initial render (Severity: Critical)

### High Priority
2. **Suspense Boundaries** — Wrap async components in Suspense boundaries (Severity: High)
3. **Conditional Loading** — Dynamic import when feature enabled (Severity: High)

### Medium Priority
4. **Lazy State Init** — Pass function to useState for expensive initial values (Severity: Medium)
5. **Memoized Components** — Extract to memo() components (Severity: Medium)
6. **Preload Intent** — Preload heavy bundles on hover/focus (Severity: Medium)
7. **Derived State** — Use derived boolean state (Severity: Medium)

### Low Priority
8. **Narrow Dependencies** — Use primitive values in dependency arrays (Severity: Low)
